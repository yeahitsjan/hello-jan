Simple Tkinter based tool to bulk change the created / modified date of files in a directory and all subdirectories.

Modify the dates.txt to get some kind of randomness.

Required python packages:
- filedate
- tk

Via "pip install filedate" && "pip install tk".